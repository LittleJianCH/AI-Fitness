#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_root/web"
exec nix develop path:..#web --accept-flake-config --no-update-lock-file "$@"
